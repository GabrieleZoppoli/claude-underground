---
name: line-comp
description: Compute & Pipelines — run analyses and reproducible pipelines.
---

# ⚫ Compute & Pipelines

Run analyses and reproducible pipelines.

## Ride it
- See the live stops + their status on the board (`/tube-map`); they're grouped under this line in the catalogue.
- `/tube-map-install comp` wires every stop on this line (detect → act → re-check), leaving auth/runtime stops as ⚠ needs-you with the exact next step.
- Python/R are local via Bash; single-cell/spatial servers are local installs.
