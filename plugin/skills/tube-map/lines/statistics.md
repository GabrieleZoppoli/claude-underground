---
name: line-stat
description: Statistics & Trial Design — choose and run the right statistics.
---

# 🔵 Statistics & Trial Design

Choose and run the right statistics, on this line.

## Ride it
- See the live stops + their status on the board (`/tube-map`); they're grouped under this line in the catalogue.
- `/tube-map-install stat` wires every stop on this line (detect → act → re-check), leaving auth/runtime stops as ⚠ needs-you with the exact next step.
- survival/lifelines + statsmodels are live; pair with the stats-plots journey.
